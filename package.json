{
  "name": "blaze-skill",
  "private": true,
  "description": "The public half of Blaze: the install instructions, the skill, the per-tool plugins, and the Solution Card schema.",
  "license": "MIT",
  "packageManager": "bun@1.4.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pascalorg/blaze.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bun run validate && bun run typecheck && bun run check:templates && bun run test:client",
    "test:client": "node --test plugins/client/blaze-client.test.mjs",
    "check:templates": "bun scripts/check-templates.ts",
    "validate": "bun run --cwd packages/cards validate",
    "typecheck": "bun run --cwd packages/cards typecheck"
  }
}
